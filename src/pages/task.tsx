import { FC, ReactNode } from "react"


const Task: FC = (/*props*/) : ReactNode => {

    return (
        <div className="page flex w-full h-full justify-center items-center">
            <h2>Tasks</h2>
        </div>
    )
}
export default Task
