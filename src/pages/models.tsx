import { FC, ReactNode } from "react"


const Models: FC = (/*props*/) : ReactNode => {

    return (
        <div className="flex w-full h-full justify-center items-center">
            Models content
        </div>
    )
}
export default Models
